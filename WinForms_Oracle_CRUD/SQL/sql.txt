CREATE TABLE crud(
  ID NUMBER GENERATED BY DEFAULT AS IDENTITY,
  PRODUCTNO VARCHAR2(250) UNIQUE,
  PRODUCTNAME VARCHAR2(250),
  PRICE NUMBER,
  GROUP1 VARCHAR2(250),
  EXPDATE DATE,
  STATUS VARCHAR2(250),
  PRIMARY KEY(ID)
);

INSERT INTO crud (PRODUCTNO, PRODUCTNAME, PRICE, GROUP1, EXPDATE, STATUS) VALUES ('33526','Testing',210.25,'name1',to_date('2023/11/15 12:23:15','yyyy/mm/dd hh24:mi:ss'),1)

SELECT * FROM CRUD;

DROP TABLE crud;